# L5-L6 Locked Boundary Evidence

Status: `blocked` by design  
Reviewed: 2026-07-10

The public core now defines only:

- `ExecutionGateway` dry-run protocol
- permanently blocked `HumanDecision`
- secret-free config diff
- no-side-effect dry-run proof
- locked live risk contract

It does not implement external transaction transport, authenticated account access, or a
credential loader. `live_gate` remains safety exit code 3. Any future unlock requires a new
explicit user instruction and an accepted venue-specific capability ADR.

Default locked caps are `min(100 USD, 0.25% equity)` notional, `min(25 USD, 0.10% equity)`
daily loss, and maximum 1x leverage. Any loss, divergence, data-health, or operational
safety breach freezes the future strategy boundary.

This evidence does not advance L5 or L6. Both remain blocked because P4 has not passed and
no venue-specific approval ADR exists.
