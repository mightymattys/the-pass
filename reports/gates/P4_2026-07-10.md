# P4 Gate Evidence

Gate: `paper_automation_and_reporting`  
Result: `blocked`  
Capability implementation: complete  
Reviewed: 2026-07-10  
Owner: Automation Engineer

## Implemented Capability

- Virtual paper worker runs as a separate credential-free process.
- Backtest and paper use the same strategy decision code, canonical events, config hash,
  fill model, and risk policy.
- Decision journal, simulated intents, fills, misses, outages, latency, costs, and risk
  events are recorded.
- Supervisor fails closed before strategy execution on stale data, clock skew, and outage
  gaps. Risk-policy rejection freezes the worker result.
- Nine scheduler-neutral AutomationSpec jobs exist. Runs use a whitelist, allowed-write
  boundary, timeout metadata, idempotency key, and schema-valid receipt. Gate and paper jobs
  cannot retry.
- Incident artifacts preserve timeline, impact, evidence, freeze action, and postmortem
  state.
- Static reporting contains all nine required views plus an index, with DuckDB aggregation
  and no writable controls or web server.

## Diagnostic Observation

The isolated Donchian replay produced:

- 2 signals and 2 fills
- 0 signal divergence
- 0 fill divergence
- 0 cost divergence
- no loaded network clients
- no credentials

It covers roughly 95 minutes of synthetic replay, not a paper window.

## Blocking Evidence

The crypto paper gate requires at least 30 calendar days and either 100 fills or 500
signals. Current evidence has 0 completed days for gate purposes, 2 fills, and 2 signals.
No research gate has passed for a real package. Therefore `paper_gate` must remain blocked.

## Commands

```text
uv run --extra data --extra research python -m unittest discover -s tests -v
92 tests passed

uv run --extra data --extra research python scripts/validate_p4_framework.py
P4 framework validation passed: capability complete, paper gate correctly blocked
```

This report must not be converted to `pass` by backdating or synthetic observation. A real
elapsed window and eligible candidate are external evidence requirements.
