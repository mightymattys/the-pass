{
  "created_at": "2026-07-13",
  "data": {
    "manifest_path": "data-manifest.json",
    "minimum_history": "two bars for framework smoke only",
    "required_fields": [
      "event_time_ns",
      "receive_time_ns",
      "open",
      "high",
      "low",
      "close",
      "volume"
    ],
    "required_sources": ["public synthetic futures fixture"]
  },
  "done_when": ["diagnostic package validates and reproduces"],
  "edge": {
    "expected_failure_modes": ["tiny sample", "bar fill approximation", "no diversification"],
    "primary_family": "threshold_smoke",
    "secondary_tags": ["example", "diagnostic"],
    "thesis": "A trivial threshold strategy proves the user strategy runtime and does not claim an edge.",
    "why_now": "Provide an offline executable custom-strategy example."
  },
  "execution": {
    "fee_model": "explicit diagnostic fee configured by the user",
    "fill_model": "next bar open",
    "latency_assumption_ms": 1,
    "order_type": "paper_only",
    "slippage_model": "five adverse basis points"
  },
  "gates": {
    "live_gate": ["forbidden"],
    "paper_gate": ["not eligible"],
    "research_gate": ["real history and independent robustness required"]
  },
  "id": "example_futures_threshold_v1",
  "kill_when": ["the fixture is mistaken for market evidence"],
  "market": {
    "asset_class": "futures",
    "instruments": ["ESZ6"],
    "timeframes": ["1m"],
    "venues": ["XCME"]
  },
  "name": "Example futures threshold runtime smoke",
  "notes": "Trusted local code. Diagnostic only; no promotion claim.",
  "owner": "example-user",
  "risk": {
    "kill_switches": ["data quality failure", "accounting failure"],
    "max_drawdown": "diagnostic only",
    "max_exposure": "one virtual contract",
    "sizing_rule": "fixed one contract"
  },
  "schema_version": 2,
  "signal": {
    "decision_time_policy": "decide after receive time and fill only on a later bar",
    "inputs": ["canonical close"],
    "lookahead_controls": ["no same-event fill"],
    "transforms": ["fixed close threshold"]
  },
  "status": "research",
  "validation": {
    "holdout_policy": "none for framework smoke; promotion forbidden",
    "null_baselines": ["seeded random"],
    "parameter_sweeps": [{"threshold": ["4700"]}],
    "required_metrics": ["net pnl", "max drawdown", "turnover", "trade count"],
    "train_test_split": "diagnostic full sample"
  }
}
